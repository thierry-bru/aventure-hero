<form method="post" action="{{ path('app_aventure_delete', {'id': aventure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aventure.id) }}">
    <button class="btn">Delete</button>
</form>
