{% extends 'base.html.twig' %}

{% block title %}Alternative{% endblock %}

{% block body %}
    <h1>Alternative</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alternative.id }}</td>
            </tr>
            <tr>
                <th>TexteAmbiance</th>
                <td>{{ alternative.texteAmbiance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_alternative_index') }}">back to list</a>

    <a href="{{ path('app_alternative_edit', {'id': alternative.id}) }}">edit</a>

    {{ include('alternative/_delete_form.html.twig') }}
{% endblock %}
