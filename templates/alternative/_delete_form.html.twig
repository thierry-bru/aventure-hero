<form method="post" action="{{ path('app_alternative_delete', {'id': alternative.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alternative.id) }}">
    <button class="btn">Delete</button>
</form>
