{% extends 'base.html.twig' %}

{% block title %}Race{% endblock %}

{% block body %}
    <h1>Race</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ race.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ race.libelle }}</td>
            </tr>
            <tr>
                <th>Bonus_force</th>
                <td>{{ race.bonusForce }}</td>
            </tr>
            <tr>
                <th>Bonus_agilite</th>
                <td>{{ race.bonusAgilite }}</td>
            </tr>
            <tr>
                <th>Bonus_endurance</th>
                <td>{{ race.bonusEndurance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_race_index') }}">back to list</a>

    <a href="{{ path('app_race_edit', {'id': race.id}) }}">edit</a>

    {{ include('race/_delete_form.html.twig') }}
{% endblock %}
